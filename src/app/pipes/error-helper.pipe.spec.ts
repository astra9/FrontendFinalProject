import { ErrorHelperPipe } from './error-helper.pipe';

describe('ErrorHelperPipe', () => {
  it('create an instance', () => {
    const pipe = new ErrorHelperPipe();
    expect(pipe).toBeTruthy();
  });
});
